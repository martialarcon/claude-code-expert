# AI Architect v2 Dependencies
# Target: Jetson Orin Nano (ARM64)

# Core
pyyaml>=6.0
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0

# HTTP & Parsing
httpx>=0.25.0
anthropic>=0.40.0
feedparser>=6.0
beautifulsoup4>=4.12

# GitHub
PyGithub>=2.0

# Vector Store
chromadb>=0.4.0

# CLI (Claude)
# Claude CLI should be installed separately
# See: https://docs.anthropic.com/claude/docs/claude-cli

# Logging
structlog>=23.0

# Utilities
tenacity>=8.0          # Retry logic
dateparser>=1.1        # Flexible date parsing
python-slugify>=8.0    # URL-safe slugs
